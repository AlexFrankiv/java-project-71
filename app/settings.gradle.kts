pluginManagement {
    repositories {
        gradlePluginPortal() // Этот репозиторий содержит плагин versions
        mavenCentral()
        google()
    }
}


rootProject.name = "app"
include("app")